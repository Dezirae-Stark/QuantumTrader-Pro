/// Common trading enums used across the application
enum TradeDirection { buy, sell }

enum TradingStatus { idle, analyzing, executing, monitoring }

enum TradingMode { conservative, aggressive }

enum TrendDirection { bullish, bearish, neutral }